%h1 Chatroom Instances
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @chatroom_instances.each do |chatroom_instance|
      %tr
        %td= chatroom_instance.name
        %td= link_to 'Show', chatroom_instance
        %td= link_to 'Edit', edit_chatroom_instance_path(chatroom_instance)
        %td= link_to 'Destroy', chatroom_instance, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Chatroom Instance', new_chatroom_instance_path
